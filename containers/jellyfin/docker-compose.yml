version: "3.9"
name: jellyfin_stack
services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    networks:
      - default
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=CET
    volumes:
      - ${CONFIG_PATH}:/config
      - ${CACHE_PATH}:/cache
      - ${MEDIA_PATH}:/media
    ports:
      - "8096:8096"
    restart: "no"

networks:
  default:
    external: true
    name: ${NETWORK}
